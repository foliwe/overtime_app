<div class="container-fluid">
	<div class="row">
		<div class="col-md-5 jumbotron">
		<h3>Request Overtime</h3>
			<%= link_to "Request Overtime Approval", new_post_path, class: "btn btn-success btn-block"%>
		</div>
		<div class="col-md-5 jumbotron">
		<%if @pending_audit_confirmations.count > 0 %>
		<h3>Pending Your Confirmations</h3>
		<%= render partial: 'pending_confirmations',locals:{pending_audit_confirmations: @pending_audit_confirmations}%>
		<%else %>
		<h3>No Overtime Request Available</h3>
		<% end %>

		</div>
	</div>
</div>